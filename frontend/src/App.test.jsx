// import { render, screen } from '@testing-library/react';
// import React from 'react';
// import App from './App';

// TODO: App component cannot be tested yet as it is still missing most of the required components.
describe('App component', () => {
	test('renders a RestaurantsDashboard component', () => {
		// TODO: For now, I added 1 assertion to make the test file run when doing `npm test`
		// render(<App />);
		expect(1).toBe(1);
		// const linkElement = screen.getByText(/learn react/i);
		// expect(linkElement).toBeInTheDocument();
	});
});
